
trigger: 
  branches:
    include: 
    - develop
    - master
pr:
  branches:
    include:
      - "*"

pool:
  vmImage: "ubuntu-latest"

#variables:
  #DEBUG: true
  #${{ if or(eq(variables['Build.SourceBranchName'], 'develop'),eq(System.PullRequest.TargetBranch, 'develop') ) }}:
  #  STACK: dev
  #  ENVIRONMENT: infra-dev
  #${{ if or(eq(variables['Build.SourceBranchName'], 'master'), eq(System.PullRequest.TargetBranch, 'master') ) }}:
  #  STACK: prod
  #  ENVIRONMENT: infra-prod

stages:
  - stage: setEnvironment
    displayName: Set environment
    jobs:
      - job: setEnv
        displayName: Preview infra (pulumi)
        cancelTimeoutInMinutes: 7
        steps:
          - script: | 
              echo $(System.PullRequest.TargetBranch)
              echo $(Build.SourceBranchName)
              if [["$(Build.SourceBranchName)" == "master" || "$(System.PullRequest.TargetBranch)" == "master"]]
              then
                echo "branch: master"
                echo "##vso[task.setvariable variable=ENVIRONMENT;isOutput=true]prod"
                echo "##vso[task.setvariable variable=STACK;isOutput=true]prod"
              fi
              if [["$(Build.SourceBranchName)" == "develop" || "$(System.PullRequest.TargetBranch)" == "develop"]]
                echo "branch: develop"
                echo "##vso[task.setvariable variable=ENVIRONMENT;isOutput=true]dev"
                echo "##vso[task.setvariable variable=STACK;isOutput=true]dev"
              fi
            name: setEnv
              
  - stage: preview
    displayName: Preview infra
    variables:
      - group: common
      - name: ENVIRONMENT
        value: $[ stageDependencies.setEnvironment.setEnv.outputs['setEnv.ENVIRONMENT'] ]
      - name: STACK
        value: $[ stageDependencies.setEnvironment.setEnv.outputs['setEnv.STACK'] ]
      
    jobs:
      - job: previewInfra
        displayName: Preview infra (pulumi)
        cancelTimeoutInMinutes: 7
        steps:
          - script: | 
              echo $(STACK)
              echo $(ENVIRONMENT)
              echo $(System.PullRequest.TargetBranch)
              echo $(Build.SourceBranchName)
          - task: Npm@1
            displayName: Pulumi npm install
            inputs:
              command: install
          - task: Pulumi@1
            displayName: Pulumi infra preview
            inputs:
              azureSubscription: "Azure thesis resource manager"
              command: "preview"
              stack: $(STACK)

#  - stage: deploy
#    displayName: Apply infra (pulumi up)
#    dependsOn: preview
#    condition: ${{or( eq(variables['Build.SourceBranchName'], 'develop'), eq(variables['Build.SourceBranchName'], 'master') )}}
#    variables:
#      - group: common
#    jobs:
#      - deployment: deployFrontendAndBackend
#        displayName: Deploy frontend and backend
#        environment: ${{variables.ENVIRONMENT}}
#        strategy:
#          runOnce:
#            deploy:
#              steps:
#                - checkout: self
#                - task: Npm@1
#                  displayName: Pulumi npm install
#                  inputs:
#                    command: install
#                - task: Pulumi@1
#                  displayName: Pulumi infra preview
#                  inputs:
#                    azureSubscription: "Azure thesis resource manager"
#                    command: "up"
#                    args: --yes
#                    stack: ${{variables.STACK}}
#